image: "ruby:2.3"

cache:
  paths:
    - vendor

stages:
  - test
  - deploy

before_script:
  - bundle install -j $(nproc) --path vendor

rubocop:
  stage: test
  script:
    - rubocop

rspec:
  stage: test
  script:
    - bundle exec rspec