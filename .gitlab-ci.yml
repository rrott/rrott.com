image: "ruby:2.4.1"

cache:
  paths:
    - vendor

stages:
  - test
  - deploy

before_script:
  - bundle install -j $(nproc) --path vendor

rspec:
  stage: test
  only:
    - master
  script:
    - bundle exec rspec
