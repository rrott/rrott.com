image: "ruby:2.3"

cache:
  paths:
    - vendor

stages:
  - test
  - deploy

before_script:
  - bundle install -j $(nproc) --path vendor

rubocop:
  stage: test
  only:
    - master
  script:
    - rubocop

rspec:
  stage: test
  only:
    - master
  script:
    - bundle exec rspec